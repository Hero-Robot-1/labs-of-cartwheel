import { WidgetCallbacks } from '../types';
export declare const ModalMessagingService: {
    send: {
        close: (payload?: any) => void;
        error: (payload?: any) => void;
        success: (payload?: any) => void;
    };
    listen: (modalId: string, callbacks?: WidgetCallbacks<any>) => void;
    unlisten: (modalId: string, callbacks?: WidgetCallbacks<any>) => void;
};
