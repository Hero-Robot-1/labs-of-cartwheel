import { Cache } from './types';
declare enum CacheSource {
    disk = "disk"
}
declare const CacheFactory: {
    make: (source: CacheSource) => Cache;
};
export { Cache, CacheFactory, CacheSource };
