import { NftPurchaseBackendPayload } from '@paytweed/shared-sdk';
import { TweedBackendSDKOptions } from './types';
declare const Middleware: {
    setup: (options: TweedBackendSDKOptions) => {
        process: (message: string, userId: string, userEmail: string) => Promise<string>;
    };
};
declare const forTests: {
    validateNftPurchaseData: (data: NftPurchaseBackendPayload) => boolean;
};
export { Middleware, forTests };
