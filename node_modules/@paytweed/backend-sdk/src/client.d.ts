import { Environment } from '@paytweed/shared-sdk';
import { getSdk } from './graphql.generated';
type Client = ReturnType<typeof getSdk>;
export declare const getClient: (apiKey: string, apiSecret: string, environment: Environment) => Promise<Client>;
export {};
